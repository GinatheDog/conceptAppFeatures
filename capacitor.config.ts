import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.conceptappfeatures',
  appName: 'conceptappfeatures',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
